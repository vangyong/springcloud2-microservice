spring:
  application:
    name: microservice-filecenter
  cloud:
    config:
      profile: dev 
      label: master
      discovery: 
        enabled: true
        serviceId: microservice-config
eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/